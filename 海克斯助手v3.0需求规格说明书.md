# 海克斯助手 v3.0 — 需求规格说明书

> **版本**：v3.0-alpha  
> **日期**：2026年6月4日  
> **状态**：待确认  
> **前置阅读**：`hexdata接入可行性研究报告.md` `海克斯助手全面检测报告.md`  

---

## 目录

1. [版本变更概述](#1-版本变更概述)
2. [需求1：符文池同步改造（双源Tab对比）](#2-需求1)
3. [需求2：AI双源比对](#3-需求2)
4. [需求3：装备池同步](#4-需求3)
5. [需求4：装备推荐同步](#5-需求4)
6. [需求5：前台装备展示](#6-需求5)
7. [需求6：前台英雄排行](#7-需求6)
8. [需求7：符文-装备联动推荐](#8-需求7)
9. [需求8：推荐配置中装备管理](#9-需求8)
10. [需求9：AI智能体卡片样式统一](#10-需求9)
11. [需求10：数据源命名规范](#11-需求10)
12. [实施优先级与依赖关系](#12-实施优先级)
13. [AI比对设计决策（已确认）](#13-ai比对设计决策已确认)
14. [存疑事项（已确认）](#14-存疑事项已确认)
15. [最终决策补充](#15-最终决策补充2026-06-04-审查后确认)

---

## 1. 版本变更概述

### 1.1 数据源调整

| 变更前 | 变更后 | 原因 |
|--------|--------|------|
| 📡 CDragon (官方) | ❌ 移除 | 无中文名、无描述、无胜率，比对价值低 |
| 🌐 arammayhem (社区) | 🌐 arammayhem (社区) | 保留，中文名+描述+666组合 |
| — | 📊 hexdata (数据站) | 新增，真实胜率+装备推荐+版本趋势 |

### 1.2 新增模块

```
v2.0 现有                        v3.0 新增
─────────────────────────        ─────────────────────────
符文池同步（双源）           →   符文池同步（双源Tab对比）
AI 比对（残缺）              →   AI 双源比对（社区vs数据站）
—                            →   装备池同步
—                            →   装备推荐同步
—                            →   前台装备Tab
—                            →   符文-装备联动提示
推荐配置（仅符文）            →   推荐配置（符文+装备）
```

---

## 2. 需求1：符文池同步改造（双源Tab对比）

### 2.1 功能描述

符文池同步从"双源（CDragon+arammayhem）合并对比"改为"两个独立来源分别与数据库对比"，通过Tab切换查看各来源的结果。

### 2.2 数据来源

| 来源 | 命名 | 数据 | 获取方式 |
|------|------|------|----------|
| 社区 | `arammayhem.com` | 195个符文（中文名+描述+品质+图标） | `search-index.json` |
| 数据站 | `hexdata.com.cn` | 199个符文（中文名+描述+品质+胜率+选取率） | `augments.json` |

### 2.3 用户界面

**预览前**：
```
┌─ ⚡ 符文池同步 ───────────────────── [195符文] ─┐
│ 双源独立对比：社区(arammayhem) + 数据站(hexdata) │
│ 分别与数据库比对，切换Tab查看各来源结果           │
│ [🔍 预览变更]  [确认更新(0)]                      │
│ ☐ 同时更新符文描述                                │
└──────────────────────────────────────────────────┘
```

**预览后**（双Tab切换）：
```
┌──────────────────────────────────────────────────┐
│ 🌐 P26.11: 195  │  📊 P16.11: 199  │  💾 195 │
│ [🌐 社区(arammayhem)] [📊 数据站(hexdata)]       │
│ ▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔   │
│ [全部] [新增(N)] [更新(N)] [停用(N)]              │
│                                                   │
│ ☑ 坚若磐石  每当你定身或缚地...   ✓社区  ✓数据站  │
│ ☐ 高压锅    无描述，待补充         —       ✓数据站 │
│ ...                                               │
└──────────────────────────────────────────────────┘
```

### 2.4 处理逻辑

```
1. 用户点击"预览变更"
2. 同时启动两个请求：
   ├─ fetch armmayhem search-index.json
   └─ fetch hexdata augments.json
3. 哪个先返回 → 立即展示该来源的Tab（默认激活）
4. 另一个返回后 → 静默填充另一个Tab的数据
5. 未返回时 → Tab显示"⏳ 数据加载中..."
6. 两个Tab各自独立地与数据库对比：
   - 社区Tab：arammayhem vs DB
   - 数据站Tab：hexdata vs DB
7. 每个Tab内的筛选/勾选/全选逻辑独立
8. 执行更新时，可分别确认社区更新和数据站更新
```

### 2.5 输出

- 社区Tab：新增/更新/停用列表（与现有逻辑相同）
- 数据站Tab：新增/更新/停用列表（与现有逻辑相同）
- 两者的差异在AI比对中展示

---

## 3. 需求2：AI双源比对

### 3.1 功能描述

社区和数据站两个来源获取完毕后，触发比对，按**三个类别**分类展示所有差异。管理员可对每行差异进行**反馈**，反馈结果存储后自动优化后续比对（避免重复比对已知的不同符文）。

#### 三个差异类别

| 类别 | 判断条件 | AI是否参与 | 示例 |
|------|---------|-----------|------|
| **类别1：同名异述** | 名称完全一致，但描述文本不同 | ❌ 纯算法 | 社区&数据站都叫"珠光护手"，但描述文字不同 |
| **类别2：异名似述** | 名称不同，但描述语义相似 | ✅ AI语义判断 | 社区"巨人杀手"≠ 数据站"歌利亚巨人"，但效果接近 |
| **类别3：独有符文** | 一方有，另一方没有 | ❌ 纯算法 | 社区有"慢炖"，数据站没有 |

#### AI Token 消耗评估

```
假设场景：双源各200个符文，首轮全量比对

类别1（同名异述）：~150对 → 不需要AI → 0 token
类别2（异名似述）：~30对需要AI
  每对 ~400 input + 50 output tokens
  30对 ≈ 13,500 tokens ≈ ¥0.02

后续重复比对：用户反馈后，已标记的跳过
  稳定后：~5对 → ~2,000 tokens ≈ ¥0.003/次

月度成本（4次/月）：< ¥0.1
```

**结论：Token消耗极低**。大部分比对不需要AI，类别2经反馈优化后AI调用量持续递减。

#### 反馈数据表

```sql
CREATE TABLE ai_comparison_feedback (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  community_name TEXT NOT NULL DEFAULT '',
  data_station_name TEXT NOT NULL DEFAULT '',
  feedback_type TEXT NOT NULL,
  -- 'same_rune'（同一符文） | 'different_runes'（不同符文）
  -- | 'prefer_community'（偏好社区） | 'prefer_data_station'（偏好数据站）
  -- | 'general_rule'（通用规则） | 'correction'（纠正AI判断）
  admin_note TEXT DEFAULT '',         -- 管理员的具体意见/优化建议
  rune_pair_key TEXT DEFAULT '',      -- 用于快速匹配："community_name||data_station_name"
  is_active BOOLEAN DEFAULT true,     -- 是否生效
  usage_count INTEGER DEFAULT 0,      -- 被引用次数（统计用）
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX idx_feedback_pair ON ai_comparison_feedback(rune_pair_key);
CREATE INDEX idx_feedback_type ON ai_comparison_feedback(feedback_type);
```

#### 反馈类型详解

| feedback_type | 含义 | 管理员操作 | 后续AI行为 |
|---------------|------|-----------|-----------|
| `same_rune` | 这两个名称不同，但是同一符文 | 勾选"[ ] 这是同一符文" | 下次比对时，AI直接采用管理员的结论 |
| `different_runes` | 这两个名称不同，确实是不同符文 | 勾选"[ ] 这是不同符文" | 下次跳过这对比对，直接归入类别3 |
| `prefer_community` | 偏好社区名称/描述 | 多次选择"采用社区" | AI在判断时增加社区权重 |
| `prefer_data_station` | 偏好数据站名称/描述 | 多次选择"采用数据站" | AI在判断时增加数据站权重 |
| `correction` | 纠正AI的错误判断 | 填写"你的判断不对，正确的是..." | AI将纠正意见作为后续决策依据 |
| `general_rule` | 通用规则建议 | 填写"稀有度不一致时，以数据站为准" | AI将规则作为永久Prompt指令 |

#### 反馈如何影响AI决策

AI每次比对时，**最近50条有效反馈**会作为Prompt的一部分发给DeepSeek：

```
你是海克斯符文比对助手。
请分析以下符文是否属于同一符文。

管理员历史决策参考（请优先遵循）：
1. [different_runes] "巨人杀手" ≠ "歌利亚巨人"：管理员确认是不同的符文
2. [prefer_community] 名称冲突时优先使用社区中文名
3. [general_rule] 稀有度不一致时，以数据站（hexdata）的稀有度为准
4. [correction] 上次"珠光护手"的判断有误：描述中"暴击伤害145%"和"暴击145%伤害"是同一效果，非差异

现在请分析：
社区符文：xxx
数据站符文：yyy
```

**Token成本**：50条反馈 ≈ 约1000 tokens，增加到每次AI调用中。每次调用额外增加约 ¥0.001。几乎可忽略。

#### 管理员反馈界面

```
┌─ 🤖 AI双源比对 ─ 类别2：异名似述 ────────────────┐
│                                                    │
│ ☐ 巨人杀手 vs 歌利亚巨人                           │
│   🤖AI分析：描述核心效果不同 → 可能为不同符文      │
│   [采用社区] [采用数据站]                           │
│                                                    │
│   📝 反馈意见（可选）：                            │
│   ┌──────────────────────────────────────────┐    │
│   │ 这两个是完全不同的符文，效果和机制都不同    │    │
│   └──────────────────────────────────────────┘    │
│   [x] 标记为不同符文，后续不再比对                  │
│   [ ] 标记为同一符文，以 [社区 / 数据站] 为准      │
│   [ ] 补充通用规则：___________                    │
└────────────────────────────────────────────────────┘
```

**反馈的三个层次**：
1. **针对本对**："这是不同符文" / "这是同一符文" → 快速勾选
2. **补充意见**：文字说明为什么这样判断 → AI下次参考
3. **上升为规则**："稀有度冲突时以数据站为准" → 永久生效

### 3.2 用户界面

```
┌─ 🤖 AI双源比对 ─ 112项差异 ──────────────────────┐
│                                                    │
│ 📊 类别1：同名异述 (85项)                           │
│ ┌──────────────────────────────────────────────┐  │
│ │ ☐ 珠光护手                                    │  │
│ │   社区描述：技能可以暴击，造成145%伤害...      │  │
│ │   数据站描述：技能可暴击，造成145%额外伤害...   │  │
│ │   [采用社区] [采用数据站]                       │  │
│ └──────────────────────────────────────────────┘  │
│                                                    │
│ 📊 类别2：异名似述 🤖AI辅助 (12项)                 │
│ ┌──────────────────────────────────────────────┐  │
│ │ ☐ 巨人杀手 vs 歌利亚巨人                       │  │
│ │   社区：巨人杀手(silver) — 获得%攻击力，体型缩小│  │
│ │   数据站：歌利亚巨人(gold) — 体型变大，生命值+   │  │
│ │   🤖AI：描述核心效果不同(攻击力 vs 生命值)      │  │
│ │         → 可能为不同符文，建议人工确认           │  │
│ │   [采用社区] [采用数据站]                       │  │
│ │   反馈：[ ] 这两个是不同的符文，后续不再比对     │  │
│ └──────────────────────────────────────────────┘  │
│                                                    │
│ ┌──────────────────────────────────────────────┐  │
│ │ ☐ 慢炖 vs 高压锅                               │  │
│ │   社区：慢炖(gold) — 每秒对附近敌人施加灼烧     │  │
│ │   数据站：高压锅(gold) — 每秒施加可叠加灼烧      │  │
│ │   🤖AI：描述核心效果相同(持续灼烧) → 同一符文    │  │
│ │   [采用社区] [采用数据站]                       │  │
│ │   反馈：[ ] 这是同一符文，社区名为准              │  │
│ └──────────────────────────────────────────────┘  │
│                                                    │
│ 📊 类别3：独有符文 (15项)                          │
│ ┌──────────────────────────────────────────────┐  │
│ │ ☐ 数据站独有：高压锅                           │  │
│ │   描述：...                                    │  │
│ │   [新增到数据库] [忽略]                         │  │
│ └──────────────────────────────────────────────┘  │
│                                                    │
│ ☑ 采用社区(45)  ☑ 采用数据站(23)  ☑ 新增(5)     │
│ [批量执行已勾选]   [继续比对第2批]                 │
└────────────────────────────────────────────────────┘
```

### 3.3 处理逻辑

```
1. 两个Tab都加载完毕后，"AI比对"按钮可用
2. 三个类别分别处理：

   类别1：同名异述（纯算法）
   ├─ 集合交集：两个来源名称完全一致的符文
   ├─ 逐个对比描述文本（字符串diff）
   ├─ 描述不同的 → 展示对比
   ├─ 描述相同的 → 跳过（无差异）
   └─ 0 token消耗

   类别2：异名似述（AI辅助）
   ├─ 先查 feedback 表：已标记"不同符文" → 直接跳过，归入类别3
   ├─ 两个来源名称不同但可能描述相似的配对
   ├─ 调用 DeepSeek 做语义比对（15对/批）
   │   Prompt：判断两段符文描述是否描述同一游戏效果
   │   返回：{verdict: "same"|"different", reason, confidence}
   ├─ 展示AI分析结果 + 管理员决策
   └─ ~500 tokens/对

   类别3：独有符文（纯算法）
   ├─ 集合差集：社区独有 / 数据站独有
   ├─ 展示符文信息
   └─ 管理员决定新增或忽略

3. 逐行勾选 + 决策：
   ├─ 管理员勾选 → 选择"采用社区"或"采用数据站"
   ├─ 类别3 → 选择"新增"或"忽略"
   └─ 未勾选的行 = 不处理

4. 反馈闭环：
   ├─ 管理员勾选"这是两个不同符文，后续不再比对" → 存入 feedback
   ├─ 下次比对 → 查 feedback 表 → 直接跳过，不调用AI
   └─ 反馈越多，AI调用越少

5. 批量执行：
   ├─ 严格按勾选执行
   ├─ "采用社区"的行 → UPDATE DB为社区数据
   ├─ "采用数据站"的行 → UPDATE DB为数据站数据
   ├─ "新增"的行 → INSERT 新符文
   └─ 未勾选的行 → 绝对不动
```

### 3.4 执行规则

| 勾选决策 | 数据库操作 | 说明 |
|----------|-----------|------|
| 采用社区 | UPDATE name/desc/tier = arammayhem版本 | 同时更新 source_id |
| 采用数据站 | UPDATE name/desc/tier = hexdata版本 | 同时更新 source_id |
| 新增（类别3） | INSERT 新符文 | 标记 source='hexdata' |
| 忽略（类别3） | 无操作 | — |
| 未勾选 | **无操作** | 绝对不动，等下次决定 |

### 3.5 反馈存储位置

反馈存储在 **Supabase 云数据库** 的 `ai_comparison_feedback` 表中。不存本地——所有管理员共享同一套反馈数据。云端存储的好处是：换设备、换浏览器、多人协作时，反馈不丢失。

### 3.6 AI比对完整逻辑流程

```
用户点击"AI比对"
        │
        ▼
┌─ 第一阶段：算法预筛选（不调AI，0 token） ──────────┐
│                                                      │
│ 1. 从 Supabase 加载 feedback 表全部有效记录          │
│ 2. 构建双源符文映射（按名称+描述匹配）               │
│                                                      │
│ 3. 分类：                                            │
│   ├─ 同名 → 类别1（同名异述）                        │
│   ├─ 异名 → 继续判定                                 │
│   │   ├─ 在 feedback 中标记为 same_rune → 类别1      │
│   │   ├─ 在 feedback 中标记为 different_runes        │
│   │   │   → 各自归入类别3（独有符文，不再比对）       │
│   │   └─ 不在 feedback 中 → 进入第二阶段              │
│   └─ 单方独有 → 类别3                                │
└──────────────────────────────────────────────────────┘
        │
        ▼
┌─ 第二阶段：AI语义比对（调DeepSeek，仅对未标记对）───┐
│                                                      │
│ 4. 构建 AI Prompt：                                  │
│    ├─ System：比对规则 + 管理员偏好                  │
│    ├─ 历史反馈（最近50条，含general_rule、correction）│
│    └─ 待比对符文对（15对/批）                        │
│                                                      │
│ 5. 调用 DeepSeek → 返回每对的判定结果                │
│    {verdict, confidence, reason, recommended_name}   │
│                                                      │
│ 6. AI 的判定中已经融入了管理员的偏好：               │
│    - "稀有度冲突时以数据站为准" → AI自动应用         │
│    - "社区中文名更准确" → AI倾向推荐社区名            │
│    - 上次纠正意见 → AI修正类似判断                    │
└──────────────────────────────────────────────────────┘
        │
        ▼
┌─ 第三阶段：展示 + 管理员确认 ───────────────────────┐
│                                                      │
│ 7. 展示三个类别，每行包含：                           │
│    - 双方数据对比                                    │
│    - AI分析结果（如适用）                            │
│    - 操作按钮 + 反馈输入框                           │
│                                                      │
│ 8. 管理员逐行确认/修正/反馈                          │
│                                                      │
│ 9. 提交后：                                          │
│    ├─ 勾选的执行行 → 写入数据库                      │
│    ├─ 反馈内容 → 写入 feedback 表（Supabase）        │
│    └─ 未勾选的行 → 不处理                            │
└──────────────────────────────────────────────────────┘
        │
        ▼
┌─ 第四阶段：下次比对时生效 ──────────────────────────┐
│                                                      │
│ 下次点击"AI比对"→ 回到第一阶段                       │
│ feedback 表已包含上次的管理员意见 → 自动应用          │
│                                                      │
│ 效果：                                               │
│ - "不同符文"标记的对 → 不再比对，直接跳过             │
│ - general_rule → 作为永久Prompt指令                   │
│ - correction → AI重新审视类似情况                    │
│ - prefer_xxx → AI自动偏向该来源                      │
│                                                      │
│ AI调用量持续递减：30对 → 20对 → 10对 → 2-3对        │
└──────────────────────────────────────────────────────┘
```

### 3.7 反馈生效示例

**第1次比对**：
- 算法筛出 30 对进入 AI 比对
- AI 判断"巨人杀手 vs 歌利亚巨人：描述相似度78%，可能同一符文"
- 管理员反馈：❌ 这是不同符文 → 写入 `different_runes`
- 管理员补充规则："稀有度冲突时以数据站为准" → 写入 `general_rule`

**第2次比对**（一周后，版本更新）：
- 算法加载 feedback → 发现"巨人杀手 vs 歌利亚巨人"已标记不同 → 跳过，不调 AI
- 新出现的符文对 → 调 AI
- AI 的 Prompt 中自动包含了："稀有度冲突时以数据站为准" → 这次判断稀有度不同的符文对时，直接采用数据站的稀有度
- 管理员确认 20 对减少到 15 对

**第10次比对**（稳定后）：
- 绝大多数配对已标记
- 新出现的类别2差异仅 2-3 对
- AI 调用几乎免费
- 管理员偏好已完全融入 AI 判断，大部分结果无需修改

---

## 4. 需求3：装备池同步

### 4.1 功能描述

从 hexdata 同步装备基础数据（名称、图标、描述）到本地数据库，建立装备基础表。类似符文池同步的装备版本。

### 4.2 数据来源

| 字段 | 来源 | 说明 |
|------|------|------|
| 装备ID | hexdata `hero_formula_items.json` | 如 3084 = 心之钢 |
| 装备名称 | CDragon `items.json` | 官方中文名 |
| 装备图标 | CDragon CDN | `raw.communitydragon.org/latest/plugins/.../items/{id}.png` |
| 装备描述 | CDragon `items.json` | 官方中文描述 |
| 装备价格 | CDragon `items.json` | 总价 |

### 4.3 数据库设计

```sql
CREATE TABLE equipment (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  game_id TEXT NOT NULL UNIQUE,    -- 游戏内ID，如 "3084"
  name TEXT NOT NULL,              -- 中文名
  description TEXT DEFAULT '',     -- 装备描述
  icon_url TEXT DEFAULT '',        -- 图标URL
  price INTEGER DEFAULT 0,         -- 价格
  is_active BOOLEAN DEFAULT true,
  source TEXT DEFAULT 'cdragon',
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### 4.4 用户界面

```
┌─ 🗡️ 装备池同步 ──────────────────────────────────┐
│ 从CDragon/hexdata获取装备基础数据                  │
│ 含名称、图标、描述、价格，版本更新时自动同步        │
│ [预览装备变更]  [确认更新(N)]                       │
│                                                    │
│ ┌─ 预览列表（与符文池同步格式一致）─────────────┐  │
│ │ ☑ 心之钢  图标  价格3200  描述:[...]  新增     │  │
│ │ ☐ 霸王血铠 图标  价格3000  描述:[...]  更新     │  │
│ └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

### 4.5 处理逻辑

```
1. 从 CDragon items.json 获取全量装备数据
2. 与本地 equipment 表对比
3. 新增/更新/标记下架
4. 图标URL存储CDragon CDN地址
```

---

## 5. 需求4：装备推荐同步

### 5.1 功能描述

从 hexdata 的 `hero_formula_items.json` 获取每个英雄的装备推荐（出门装+核心装），自动创建或更新到该英雄的"通用流派"中。

### 5.2 数据来源

hexdata `hero_formula_items.json`：
```json
{
  "heroId": "875",
  "heroName": "腕豪",
  "starterItems": [{"name":"巨人腰带","id":"1011"}, {"name":"红水晶","id":"1028"}],
  "coreItems": [{"name":"心之钢","id":"3084"}, {"name":"霸王血铠","id":"2501"}, ...]
}
```

### 5.3 用户界面

与英雄-符文匹配卡片风格统一：
```
┌─ ⚔️ 装备推荐同步 ────────────────────────────────┐
│ 从hexdata获取172个英雄的装备推荐                   │
│ 同步到推荐配置的"通用流派"中                       │
│ [预览]  [确认更新(N)]                              │
│                                                    │
│ ┌─ 预览列表 ────────────────────────────────────┐  │
│ │ 英雄    出门装            核心装         变更   │  │
│ │ 腕豪    巨人腰带,红水晶   心之钢,霸王血铠 新增  │  │
│ │ 拉克丝  多兰之戒         卢登,影焰,帽子  更新  │  │
│ └───────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

### 5.4 处理逻辑

```
1. 获取 hexdata hero_formula_items.json
2. 遍历172个英雄：
   ├─ 匹配本地hero_id（通过 heroName）
   ├─ 查找/创建"通用流派" playstyle
   ├─ 将装备推荐写入 hero_equipment_recs 表
   └─ 关联 playstyle_id = 通用流派
3. 手动创建的流派不覆盖
4. 不存在于hexdata的英雄 → 跳过
```

---

## 6. 需求5：前台装备展示

### 6.1 功能描述

前台选择英雄后，在符文推荐旁边新增"装备推荐"Tab，展示该英雄的出门装和核心装备。

### 6.2 用户界面

```
┌─ 🔮 拉克丝 ──────────────────────────────────────┐
│ Lux — 光辉女郎  [AP] [爆发法师]  S级 · 52.86%    │
├────────────────────────────────────────────────────┤
│ [🪄 符文推荐]  [🗡️ 装备推荐]                       │
│                                                    │
│ ┌─ 出门装 ────────────────────────────────────┐   │
│ │ [多兰戒图标] 多兰之戒                        │   │
│ └──────────────────────────────────────────────┘   │
│                                                    │
│ ┌─ 核心装备 ──────────────────────────────────┐   │
│ │ ① [卢登图标] 卢登的伙伴   3400金币           │   │
│ │    技能命中额外溅射伤害 + 法术穿透            │   │
│ │                                              │   │
│ │ ② [影焰图标] 影焰         3200金币           │   │
│ │    对低血量敌人造成额外伤害                   │   │
│ │                                              │   │
│ │ ③ [帽子图标] 灭世者的死亡之帽 3600金币       │   │
│ │    法术强度大幅提升                           │   │
│ └──────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────┘
```

### 6.3 处理逻辑

```
1. 用户选择英雄
2. 查询 hero_equipment_recs (通过 playstyle_id 或 hero_id)
3. 展示出门装（starterItems）+ 核心装（coreItems）
4. 从 equipment 表获取图标URL和描述
```

---

## 7. 需求6：前台英雄排行（独立Tab + 搜索排序）

### 7.1 功能描述

首页新增"英雄排行"Tab，展示 172 个英雄按胜率/选取率/Tier 排序的实时排行。首页搜索按胜率降序。数据来自 hexdata `heroes.json`。

### 7.2 用户界面

**英雄排行 Tab**：
```
┌──────────────────────────────────────────────────────────────┐
│  [🦸 英雄排行] [🪄 符文推荐] [🗡️ 装备推荐]                    │
│                                                               │
│  📊 英雄排行 — Patch 16.11                                    │
│  排序：[胜率▼] [选取率] [Tier] [KDA]                           │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │ #  英雄        Tier  胜率     选取率   趋势  优选符文     │ │
│  │ 1  含羞蓓蕾     T1   57.85%   0.75%   ↑10.2  天音爆      │ │
│  │                                               属性叠属性  │ │
│  │                                               魔法飞弹    │ │
│  │ 2  复仇焰魂     T1   54.73%   1.18%   ↑2.1   炼狱导管    │ │
│  │                                               魔法飞弹    │ │
│  │                                               属性叠属性  │ │
│  │ 3  暴走萝莉     T1   54.73%   1.18%   —      暴击飞弹    │ │
│  │                                               红包        │ │
│  │                                               属性叠属性  │ │
│  └─────────────────────────────────────────────────────────┘ │
│  点击任意英雄 → 跳转到符文/装备推荐Tab                          │
└──────────────────────────────────────────────────────────────┘
```

**优选符文数据来源**：hexdata `heroes.json` 每个英雄的 `topAugments` 字段（取前3个名称）。

**首页搜索排序**（按胜率降序）：
```
搜索"克"：
┌──────────────────────────────┐
│ 🔮 含羞蓓蕾  T1  57.85% ↑    │ ← 胜率最高排第一
│ 🔮 暴走萝莉  T1  54.73% —    │
│ 🔮 光辉女郎  T2  52.86% ↓    │
│ ⚔️ 德莱厄斯  T3  49.04% —    │
└──────────────────────────────┘
```

### 7.3 处理逻辑

```
1. hexdata heroes.json → heroes 表（更新 winRate/pickRate/tier/games/kda）
2. topAugments 写入 hero_top_augments 关联表
3. 英雄排行Tab：默认按 winRate 降序，可切换排序
4. 首页搜索：过滤后按 winRate 降序
5. 趋势箭头：↑ 绿色(>0.5%) / ↓ 红色(<-0.5%) / — 灰色
6. 点击英雄 → 跳到符文/装备推荐Tab
```

### 7.4 数据库扩展

```sql
ALTER TABLE heroes ADD COLUMN IF NOT EXISTS pick_rate REAL DEFAULT 0;
ALTER TABLE heroes ADD COLUMN IF NOT EXISTS hex_games INTEGER DEFAULT 0;
ALTER TABLE heroes ADD COLUMN IF NOT EXISTS hex_kda REAL DEFAULT 0;
ALTER TABLE heroes ADD COLUMN IF NOT EXISTS win_rate_change REAL DEFAULT 0;
ALTER TABLE heroes ADD COLUMN IF NOT EXISTS hex_patch TEXT DEFAULT '';
```

---

## 8. 需求7：符文-装备联动推荐

### 8.1 功能描述

用户选择符文后，系统提示该符文与哪些装备存在联动关系，帮助玩家做出更好的出装决策。

### 7.2 联动数据来源

联动关系描述需要从现有数据中提取。方案：

| 方案 | 来源 | 可行性 |
|------|------|--------|
| A. 社区描述中提取 | arammayhem desc字段含"配合XX装备..." | ⚠️ 需解析 |
| B. 数据站描述中提取 | hexdata augments desc字段 | ⚠️ 需解析 |
| C. 人工维护映射表 | 管理员手工添加 | ✅ 最可靠 |
| D. DeepSeek生成 | AI分析符文效果+装备效果 | ⚠️ 需API |

**建议**：先用C方案（人工维护少量热门联动），配合B和D逐步扩充。

### 7.3 用户界面

```
符文卡片上：
┌─ 🔮 珠光护手 [彩色] [推荐] 95分 ────────────────┐
│ 技能可以暴击，造成145%伤害                        │
│ 💡 推荐装备：卢登的伙伴 (溅射联动)                │
│ [选择] [排除]                                     │
└──────────────────────────────────────────────────┘

装备卡片上（装备Tab）：
┌─ 卢登的伙伴 ────────────────────────────────────┐
│ 💡 适合符文：珠光护手、终极唤醒、炼狱导管        │
└──────────────────────────────────────────────────┘
```

### 7.4 数据库设计

```sql
CREATE TABLE rune_equipment_links (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  rune_id UUID REFERENCES runes(id) ON DELETE CASCADE,
  equipment_id UUID REFERENCES equipment(id) ON DELETE CASCADE,
  link_reason TEXT DEFAULT '',       -- 联动理由
  link_strength INTEGER DEFAULT 50,  -- 联动强度 0-100
  source TEXT DEFAULT 'manual',
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 9. 需求8：推荐配置中装备管理

### 9.1 功能描述

后台"推荐配置"页面新增装备字段，管理员可手动为每个英雄/流派添加装备推荐，包括**核心出装**和**替换出装**。添加时输入装备名称自动补全（显示图标+名称），鼠标悬停显示完整描述。

### 9.2 用户界面

编辑推荐配置时（RecsTab 内的玩法流派编辑区）：
```
┌─ 编辑推荐配置 ─ 拉克丝 - 法爆流派 ──────────────┐
│                                                   │
│ 🪄 符文推荐 [已有，不变]                           │
│                                                   │
│ ───────────────────────────────────────────────   │
│                                                   │
│ 🗡️ 装备推荐：                                     │
│                                                   │
│ 出门装：                                          │
│ ┌──────────────────────────────────────────────┐ │
│ │ [多兰之戒] ×                                 │ │
│ │ [+ 添加出门装]                                │ │
│ └──────────────────────────────────────────────┘ │
│                                                   │
│ 核心出装：                                        │
│ ┌──────────────────────────────────────────────┐ │
│ │ ① [卢登的伙伴] ×   ② [影焰] ×               │ │
│ │ ③ [灭世者的死亡之帽] ×  ④ [虚空之杖] ×      │ │
│ │ [+ 添加核心装]                                │ │
│ └──────────────────────────────────────────────┘ │
│                                                   │
│ 替换出装（可选）：                                │
│ ┌──────────────────────────────────────────────┐ │
│ │ [中娅沙漏] ×    [女妖面纱] ×                  │ │
│ │ [+ 添加替换装]                                │ │
│ └──────────────────────────────────────────────┘ │
│                                                   │
│ [保存流派配置]                                    │
└───────────────────────────────────────────────────┘
```

**添加装备时的自动补全弹窗**：
```
输入"卢登" →
┌─────────────────────────────────────────┐
│ [图标] 卢登的伙伴                        │
│    +80法术强度  +600法力值               │ ← 悬停显示这个
│   唯一被动—溅射：技能命中额外伤害        │
├─────────────────────────────────────────┤
│ [图标] 卢登的回声 (旧版/已移除)          │
│   ...                                   │
└─────────────────────────────────────────┘
```

### 9.3 数据存储

```sql
-- 扩展现有 hero_equipment_recs 表
ALTER TABLE hero_equipment_recs ADD COLUMN IF NOT EXISTS alternative_items JSONB DEFAULT '[]';
-- starter_items: [{name, id, icon_url}]
-- core_items: [{name, id, icon_url}]
-- alternative_items: [{name, id, icon_url}]
```

### 9.4 处理逻辑

```
1. 管理员打开玩法流派编辑区
2. 点击「+ 添加核心装」→ 输入框获得焦点
3. 输入装备名 → 防抖300ms → 搜索 equipment 表
4. 下拉列表展示匹配项：
   ├─ 每一行：图标(32px) + 名称 + 价格
   └─ 鼠标悬停：弹tooltip显示完整描述 + 属性
5. 点击选中 → 填入输入框 + 显示装备图标 + ×移除按钮
6. 支持拖拽排序（核心出装的顺序）
7. 保存到 hero_equipment_recs（关联 playstyle_id）
8. 如果没有替换出装 → 前台隐藏替换出装区域
```

---

## 10. 需求9：AI智能体卡片样式统一

### 9.1 规范

所有卡片（符文池同步、英雄胜率同步、装备池同步、装备推荐同步、英雄-符文匹配）必须遵循统一样式：

```
┌─ 卡片标题 [标签] ───────────────────────────────┐
│ 功能描述（1-2行小字）                             │
│ [预览]  [确认更新(N)]  可选配置项...              │
│                                                    │
│ ┌─ 预览区域 ──────────────────────────────────┐  │
│ │ 📊 统计栏：数据来源 + 数量 + [全选][收起]    │  │
│ │ [筛选Tab: 全部(N) 新增(N) 更新(N) 跳过(N)]   │  │
│ │                                              │  │
│ │ 表头： 列1  列2  列3  ...  变更说明           │  │
│ │ ─────────────────────────────────────────    │  │
│ │ ☑ 数据行1                                    │  │
│ │ ☐ 数据行2                                    │  │
│ │ ...                                          │  │
│ └──────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

### 9.2 统一元素

| 元素 | 规范 |
|------|------|
| 表头 | `text-[11px] font-semibold text-sage-500 bg-sage-50/50 border-b border-sage-200/50` |
| 数据行 | hover高亮，交替行底色（偶数行稍深） |
| 筛选Tab | 圆角药丸，选中=金色，未选中=灰色 |
| 全选按钮 | 单按钮切换式（全选↔取消全选） |
| 收起按钮 | 右上角，展开/收起切换 |
| 进度条 | 弹窗居中，百分比实时 + 脉冲动画兜底 |
| 无变更提示 | 绿色背景"✅ 经过检测，目前没有需要变更的" |
| 执行按钮 | `确认更新(N)` ，0项时禁用 |

---

## 11. 需求10：数据源命名规范

| 内部名称 | 用户可见名称 | 标识色 | Tab标签 |
|----------|-------------|--------|---------|
| arammayhem | 🌐 社区 | 绿色 `#10B981` | `🌐 社区(arammayhem)` |
| hexdata | 📊 数据站 | 蓝色 `#3B82F6` | `📊 数据站(hexdata)` |
| CDragon | ~~官方~~（移除） | — | — |
## 12. 实施优先级与依赖关系（修订）

```
Phase 1 (基础) — 3天
├─ N1: 符文池同步双源Tab改造            [1.5天]
│   依赖：hexdata数据获取模块
├─ N2: AI双源比对 + feedback表          [1天]
│   依赖：N1
└─ N3: AI智能体布局统一                 [0.5天]
    依赖：N1

Phase 2 (装备) — 3天
├─ N4: 装备池同步（CDragon装备数据）     [1天]
│   依赖：CDragon items.json解析
├─ N5: 装备推荐同步（hexdata）           [1天]
│   依赖：N4 + hexdata hero_formula_items.json
└─ N6: 推荐配置装备管理                  [1天]
    依赖：N4

Phase 3 (前台) — 3.5天
├─ N7: 前台装备Tab                      [1天]
│   依赖：N5
├─ N8: 符文-装备联动                     [1天]
│   依赖：N4 + N5 + 联动数据
├─ N9: 前台英雄排行Tab                   [1天]
│   依赖：hexdata heroes.json
└─ N10: 首页搜索按胜率排序               [0.5天]
    依赖：N9

Phase 4 (收尾) — 1天
├─ 符文管理增加 source/时间/筛选字段      [0.5天]
└─ 全链路测试 + 文档更新                 [0.5天]
```

**总预估工时：~10.5天 ≈ 2周**
```

**总预估工时：7天**

---

## 13. AI比对设计决策（已确认）

以下 6 个问题经探讨后确认方案：

### 12.1 类别2默认不勾选 ✅

- 类别2（异名似述）的行**默认不勾选**
- 每行标记 `⚠️ AI判断，请人工确认`
- 必须手动点击才能勾上，防止误操作

### 12.2 预筛选规则 ✅

```
进入AI比对的候选对 → 必须同时满足：

条件A：名称相似度 ≥ 0.3 或 描述Jaccard相似度 ≥ 0.3

条件B：未被 feedback 标记为 different_runes

独立符文（一方完全没有匹配的）：
├─ 标注来源
├─ 显示"在其他来源未找到对应的符文"
└─ 仍进入 AI 比对（AI判断是否为独立新符文）
```

### 12.3 反馈冲突处理 ✅

- 同一符文对的最新反馈**覆盖**旧反馈
- 添加 `override_count` 计数器
- 被覆盖 ≥ 2 次 → 显示 `⚠️ 历史意见矛盾，请重新判断`
- 管理员重新反馈后重置计数器

### 12.4 版本更新清理 ✅

- hexdata `patch` 字段变化时：标记旧版本 feedback 为 `is_active=false`
- 确保 feedback 不会在版本更新后产生错误判断

### 12.5 执行后状态 ✅

- 执行后自动重新预览，确认结果
- 符文管理表新增字段：
  ```sql
  ALTER TABLE runes ADD COLUMN IF NOT EXISTS source TEXT DEFAULT '';
  ALTER TABLE runes ADD COLUMN IF NOT EXISTS last_synced_at TIMESTAMPTZ;
  ```
- 预览中显示来源标签：`来源: 社区 | 同步时间: 2026-06-03`

### 12.6 AI比对三种模式 ✅

```
AI比对按钮的可用条件：

社区Tab已加载 → 选项1可用: "社区 ↔ 数据库"
数据站Tab已加载 → 选项2可用: "数据站 ↔ 数据库"
两个Tab均已加载 → 选项3可用: "社区 ↔ 数据站（AI深度比对）"

点击AI比对按钮 → 弹出选择：
┌─ 🤖 AI比对模式 ──────────────────────────────────┐
│                                                    │
│ [🔄 社区 ↔ 数据库]  查看社区与数据库的差异          │
│ [🔄 数据站 ↔ 数据库] 查看数据站与数据库的差异        │
│ [🤖 社区 ↔ 数据站]   AI深度比对两个来源的符文差异   │
│                                                    │
│ 提示：前两项等同于预览Tab，可快速跳转               │
│       第三项需要两个来源均加载完毕                  │
└────────────────────────────────────────────────────┘
```

前两项等价于预览Tab的快捷入口，第三项才是真正的 AI 比对。

---

## 14. 存疑事项（已确认）

| 编号 | 问题 | 确认结果 |
|------|------|----------|
| Q1 | 装备图标从CDragon CDN获取是否稳定？ | ✅ 先试CDragon。如果不稳定，爬取社区/数据站的装备图标（必须高清）。不降低质量标准。 |
| Q2 | 符文-装备联动关系数据从何而来？ | ✅ 从社区(arammayhem)的推荐理由中提取联动描述 + 搜索引擎查询玩家社区推荐内容。AI辅助整理。 |
| Q3 | hexdata装备ID是否与CDragon一致？ | ✅ 先对比验证。一致则直接用。不一致以CDragon官方ID为准。获取不到官方数据时先用数据站的。 |
| Q4 | 手动修改过的符文名称是否需要保护？ | ✅ 不保护。一切以同步为准。 |
| Q5 | 装备版本更新（价格调整等）是否需要追踪？ | ✅ 需要追踪。装备描述更新、价格调整都需要记录变更。 |
| Q6 | 符文rarity的权威来源？ | ✅ 不设权威。双源都获取，通过AI比对。两源一致则通过，不一致的标记出来人工处理。 |
| Q7 | 双Tab加载策略？ | ✅ 优先获取社区（arammayhem），其次获取数据站（hexdata）。加载不阻塞其他操作。超过8秒弹提示。不设降级——必须两个都加载。 |

---

> 📌 **使用说明**：开发时严格按照此文档执行。以上问题已全部确认，开发时直接按确认结果实现。如遇到文档中未覆盖的新情况，**优先暂停并询问确认**，不要自行假设。

---

## 15. 最终决策补充（2026-06-04 审查后确认）

### 15.1 CDragon 定位

- **符文来源**：❌ 完全移除
- **装备数据**：✅ 保留。CDragon items.json 提供装备名称、图标（CDN）、描述、价格。先接入验证准确性，如不稳定再爬取数据站/社区。

### 15.2 装备数据源决策链

```
装备名称/描述/图标获取优先级：
1. hexdata（数据站）→ 有则用
2. 爬取不到 → 检测hexdata数据来源（是否引用CDragon）
3. 来源是官方 → 从CDragon获取
4. 来源不是官方 → 尝试爬取
5. 爬取不到 → 从arammayhem（社区）获取
6. 都获取不到 → 只显示装备名，标注"描述暂缺"

前端展示来源标签："数据来源：hexdata / CDragon / 社区"
```

### 15.3 Q7/12.6 加载策略最终版

```
1. 优先加载社区（arammayhem），8s内返回
2. 社区加载完 → 开始加载数据站（hexdata），同时社区Tab已可用
3. 数据站加载中 → Tab显示"⏳ 数据站加载中...预计还需X秒"
4. 超过30s → 提示"数据站响应较慢，是否继续等待？[继续等待] [仅比对社区]"
5. 超过2分钟 → 自动判定为不可达，显示"数据站暂时无法访问（可能原因：网络波动/服务器维护），建议稍后重试。当前仅社区数据可用。"
6. 仅社区可用时：选项1和预览Tab正常，选项3（社区↔数据站AI比对）禁用
```

### 15.4 AI智能体布局探讨

6张卡片竖向堆叠可能过长。备选方案：
- **折叠分组**：符文相关(2张) / 装备相关(2张) / 英雄相关(2张)，每组默认折叠
- **标签页模式**：顶部 [符文同步] [装备同步] [英雄数据] 三个标签切换
- **保持卡片但可拖拽排序**：管理员自行排列优先级

**暂定**：保持卡片，开发时先实现功能，样式后续迭代。如果觉得太长，可以快速改为折叠分组模式。

### 15.5 符文管理增加独有符文筛选

```
同步后产生的新符文（来源为数据站或社区独有）：
├─ 默认 is_active = false（不启用）
├─ 符文管理中新增筛选条件：「来源」— 社区独有 / 数据站独有 / 双源共有
├─ 管理员查看后手动启用
└─ 不启用的符文不出现在前台推荐
```

```sql
ALTER TABLE runes ADD COLUMN IF NOT EXISTS source TEXT DEFAULT '';
ALTER TABLE runes ADD COLUMN IF NOT EXISTS last_synced_at TIMESTAMPTZ;
ALTER TABLE runes ADD COLUMN IF NOT EXISTS sync_status TEXT DEFAULT '';
-- sync_status: 'active' | 'pending_review'（独有符文待审核）
```

### 15.6 执行失败处理

```
1. 全部成功 → 显示"更新完成！共处理XX条"
2. 部分成功 → 显示"XX条成功，YY条失败。建议重新点击预览确认数据状态。失败数据未写入数据库。"
3. 全部失败 → 显示"更新失败：错误信息"。不写入任何数据。
4. 不自动回滚——已成功的数据保留，失败的等下次重试。
5. 执行后自动重新预览（无论成功/失败）
```


### 15.7 英雄排行展示位置修正

英雄排行**不是**独立 Tab，而是首页英雄选择区域的右上角按钮：

```
┌──────────────────────────────────────────┐
│  🔍 搜索英雄...                  [📊 排行] │ ← 右上角按钮
│                                           │
│  搜索结果（按胜率降序）：                   │
│  🔮 含羞蓓蕾  T1  57.85% ↑                │
│  🔮 暴走萝莉  T1  54.73% —                │
│  ...                                      │
└──────────────────────────────────────────┘

点击 [📊 排行] → 弹出侧边栏/弹窗：
┌─ 📊 英雄排行 — Patch 16.11 ──────────────────┐
│ 排序：[胜率▼] [选取率] [Tier]                  │
│ ┌──────────────────────────────────────────┐ │
│ │ #  英雄        Tier  胜率    选取率  趋势  │ │
│ │ 1  含羞蓓蕾     T1   57.85%  0.75%  ↑10.2 │ │
│ │ 2  复仇焰魂     T1   54.73%  1.18%  ↑2.1  │ │
│ │ 3  暴走萝莉     T1   54.73%  1.18%  —     │ │
│ │ ...                                      │ │
│ └──────────────────────────────────────────┘ │
│ 点击任意英雄 → 关闭弹窗 + 选中该英雄          │
└──────────────────────────────────────────────┘
```

侧边栏从右侧滑出，覆盖在符文推荐上方。关闭后回到主界面，英雄已选中。
