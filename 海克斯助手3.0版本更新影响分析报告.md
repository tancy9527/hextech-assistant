# 海克斯助手 — 3.0版本更新影响分析报告

> **编制日期**：2026年6月3日
> **编制人**：海克斯助手开发团队
> **适用版本**：英雄联盟海克斯大乱斗 3.0（2026年6月11日国服更新）

---

## 目录

1. [版本更新概述](#1-版本更新概述)
2. [现有功能影响分析](#2-现有功能影响分析)
3. [新增功能需求](#3-新增功能需求)
4. [已实现功能规格说明](#4-已实现功能规格说明)
5. [掌上英雄联盟竞品分析](#5-掌上英雄联盟竞品分析)
6. [用户操作性优化建议](#6-用户操作性优化建议)
7. [行动计划](#7-行动计划)

---

## 1. 版本更新概述

海克斯大乱斗 3.0 预计 **2026年6月11日** 上线国服正式服（美测服 PBE 已上线），核心变化：

| 类别 | 变化 | 数量 |
|------|------|------|
| 羁绊系统 | **全部删除**（9个羁绊效果转为新符文） | -9 |
| 删除符文 | 白银14、黄金14、棱彩9 | -37 |
| 新增符文 | 白银13、黄金29、棱彩21 | +63 |
| 技能强化符文 | 全新类别，针对特定英雄技能 | +25 |
| 符文总数变化 | 旧195 → 新约221 | +26 |

### 关键趋势

- **符文总量增加**：从约195个增加到约221个
- **个性化增强**：25个技能强化符文使英雄build更加多样
- **羁绊系统取消**：原有的套装加成逻辑不再适用
- **匹配逻辑调整**：符文与英雄的关联更加紧密（技能强化符文绑定特定英雄）

---

## 2. 现有功能影响分析

### 2.1 符文池同步 — ✅ 可自动适配

**影响评估**：低风险

| 功能点 | 3.0兼容性 | 说明 |
|--------|-----------|------|
| arammayhem.com 数据源 | ⚠️ 待确认 | 社区网站需更新至3.0数据，预计版本更新后1-3天 |
| CDragon 官方数据 | ✅ 兼容 | 游戏文件提取，版本更新后自动同步最新数据 |
| 符文名称自动匹配 | ✅ 兼容 | source_id + name + 描述三重匹配自动处理 |
| 符文停用/新增 | ✅ 兼容 | 37个符文自动停用，63个新符文自动录入 |
| 描述获取 | ⚠️ 待确认 | 新符文中文描述需等 arammayhem 社区更新，否则需 AI 翻译 |
| 技能强化符文 | ⚠️ 需要适配 | 新增符文类别，需在数据库和UI中标明 |

**结论**：基础同步功能无需改动，但需要新增「技能强化符文」类别标识。

### 2.2 英雄-符文匹配 — ⚠️ 需要适配

**影响评估**：中风险

| 功能点 | 3.0兼容性 | 说明 |
|--------|-----------|------|
| arammayhem combo 数据 | ⚠️ 待更新 | 社区666组合数据需重新生成，旧组合可能失效 |
| 通用流派同步 | ✅ 兼容 | 新组合自动归入"通用流派" |
| 匹配度评分 | ✅ 兼容 | S/A/B/C 评分体系不变 |
| 技能强化符文匹配 | ❌ 需要新增 | 此类符文**仅特定英雄可用**，需建立英雄-符文强绑定关系 |
| 删除符文影响 | ✅ 已处理 | 符文停用后推荐自动过滤，不影响前台 |

**结论**：需新增技能强化符文的英雄专属匹配逻辑。旧推荐在符文停用后自动隐藏，无需手动清理。

### 2.3 羁绊系统 — ❌ 需要移除

**影响评估**：高影响

| 功能点 | 3.0兼容性 | 说明 |
|--------|-----------|------|
| fetters 表 | ❌ 废弃 | 9个羁绊全部删除 |
| rune_fetters 关联表 | ❌ 废弃 | 关联关系无效 |
| 羁绊加成算法 | ❌ 废弃 | `getFetterBoost()` 函数不再适用 |
| 前台羁绊展示 | ❌ 需要移除 | UI上的羁绊信息需隐藏 |

**结论**：羁绊相关功能需**整体下线**，但保留代码和数据以备未来可能恢复。

### 2.4 英雄胜率同步 — ✅ 兼容

**影响评估**：低风险

- 胜率数据来源不变（arammayhem.com）
- 英雄池不变（172个英雄）
- 技能强化符文不影响英雄胜率统计

### 2.5 前台推荐展示 — ⚠️ 需要调整

**影响评估**：中风险

| 功能点 | 3.0兼容性 | 说明 |
|--------|-----------|------|
| 符文卡片展示 | ✅ 兼容 | 现有 RuneCard 组件无需改动 |
| 品质筛选 | ✅ 兼容 | 彩色/金色/银色分类不变 |
| 羁绊展示 | ❌ 需移除 | 符文卡片上的羁绊标签需隐藏 |
| 技能强化符文展示 | ⚠️ 需新增 | 需特殊标识，区分于普通符文 |
| 基准分推算 | ⚠️ 需调整 | 评分矩阵可能需针对新符文类型优化 |

**结论**：移除羁绊展示，新增技能强化符文标识。

---

## 3. 新增功能需求

### 3.1 技能强化符文管理

#### 需求规格说明

**功能描述**  
新增「技能强化符文」类别标识，此类符文绑定特定英雄，仅在该英雄被选中时才显示推荐。

**用户界面**  
- 符文卡片上新增「技能强化」标签（区别于彩色/金色/银色品质标签）
- 标签颜色：橙色（`bg-orange-100 text-orange-600`）
- 筛选栏新增「技能强化」筛选项
- 英雄-符文匹配页面中，技能强化符文关联信息展示绑定英雄名

**输入**  
- arammayhem / CDragon 数据中的符文类别标识
- 管理员手动标注的技能强化符文-英雄绑定关系

**处理逻辑**  
1. 同步符文时，检测符文是否为技能强化类（通过名称关键词或社区数据标识）
2. 自动关联技能强化符文到对应英雄（如"强化Q技能"类符文匹配到对应英雄名）
3. 前台推荐：仅在选中对应英雄时，该技能强化符文才出现在推荐列表
4. 基准分策略：匹配英雄=90分起，不匹配英雄=0分（不显示）

**输出**  
- 符文表中新增 `is_skill_augment` 布尔字段
- 新增 `hero_skill_augments` 关联表（hero_id, rune_id）
- 前台推荐列表中的技能强化符文

---

### 3.2 羁绊系统移除

#### 需求规格说明

**功能描述**  
3.0版本删除全部羁绊，前台和数据库中的羁绊相关功能需下线。

**处理逻辑**  
1. 保留 `fetters` 和 `rune_fetters` 表结构不删除（备未来恢复）
2. 推荐算法中移除 `getFetterBoost()` 调用
3. 前台 RuneCard 中移除羁绊标签展示
4. 管理后台「羁绊管理」标签保留但标注「3.0已废弃」
5. 符文同步时不再同步羁绊数据

**输出**  
- 推荐算法简化，去除羁绊加成
- 前台UI简化

---

### 3.3 版本更新自动检测

#### 需求规格说明

**功能描述**  
自动检测 arammayhem.com 的 patch 版本变化，当检测到大版本更新时，在管理后台主动提醒管理员进行数据同步。

**用户界面**  
- AI智能体顶部状态栏：版本变化时显示警告标识
- 仪表盘新增「版本状态」卡片

**输入**  
- arammayhem.com `search-index.json` 的 `patch` 字段
- 上次同步记录的版本号（存储在 `update_logs` 表）

**处理逻辑**  
1. 每次打开 AI 智能体页面时，静默获取 arammayhem 版本号
2. 对比上次记录的版本号
3. 如不一致 → 顶部显示"⚠️ 检测到版本更新 Pxx.xx，建议同步数据"
4. 同步完成后更新记录版本

**输出**  
- 版本变化通知
- 更新日志记录

---

### 3.4 前台游戏辅助优化

#### 需求规格说明

**功能描述**  
优化前台操作流程，使用户在游戏过程中能快速完成符文选择，减少操作步骤和时间。

**用户界面**  

1. **快速模式开关**：首页顶部新增「快速模式」按钮
   - 开启后：隐藏符文描述详情，仅显示符文名+分数+品质标签，一屏显示更多符文
   - 关闭后：恢复完整卡片展示（描述+优缺点+场景）

2. **阶段计时器**：实时显示当前游戏阶段
   - 基于游戏时间自动切换 Lv.3 / Lv.7 / Lv.11 / Lv.15
   - 高亮当前阶段的推荐符文
   - 游戏计时由用户手动启动（不调用游戏API）

3. **一键排除**：符文卡片新增快速排除按钮
   - 上滑：标记为"已见过"
   - 下滑：标记为"不感兴趣"
   - 排除后的符文缩小显示在底部排除栏

**处理逻辑**  
1. 用户选择英雄 → 展示推荐列表
2. 用户点击"开始游戏" → 启动阶段计时器
3. 计时器每15秒检查一次，自动切换阶段高亮
4. 用户滑动符文卡片 → 加入排除列表
5. 排除后的符文在后续阶段不再显示

**输出**  
- 优化后的符文选择界面
- 阶段计时器和通知

---

## 4. 已实现功能规格说明

本次对话窗口过程中实现的AI智能体系统功能：

### 4.1 符文池同步（双源对比）

#### 功能描述
从两个数据源（CDragon官方 + arammayhem社区）拉取最新符文数据，与本地数据库对比，自动新增/更新/停用符文。

#### 用户界面
- 位置：管理后台 → AI智能体 → 符文池同步卡片
- 顶部展示双源版本和符文数量
- 预览列表：新增/更新/停用分组，逐行勾选确认
- 筛选标签：全部/新增/更新/停用
- 可收起/展开

#### 输入
- 📡 CDragon：`raw.communitydragon.org` 游戏文件（动态版本检测）
- 🌐 arammayhem：`arammayhem.com/search-index.json`（195个符文+中文名+描述）
- 💾 本地：Supabase runes 表

#### 处理逻辑
1. 同时拉取双源数据
2. 三重匹配策略：`source_id` → 中文名精确匹配 → 描述相似度匹配
3. CDragon 提供权威符文列表和稀有度，arammayhem 提供中文名和描述
4. 匹配成功的 → 更新 source_id、描述、图标
5. 不匹配的 arammayhem 符文 → 新增
6. 两个源都不存在的 DB 符文 → 停用
7. **安全保证**：永不修改符文名称，不删除已有推荐配置

#### 输出
- runes 表更新（新增/更新/停用）
- update_logs 表记录（仅实际执行时写入）
- 预览变更详情（不写入日志）

---

### 4.2 英雄胜率同步

#### 功能描述
从 arammayhem.com 获取172个英雄的 Meta 等级（S+~C）和胜率数据，写入 heroes 表。

#### 用户界面
- 位置：管理后台 → AI智能体 → 英雄胜率同步卡片
- 预览列表：英雄名、等级（旧→新）、胜率（旧→新）、定位、变更说明
- 逐行勾选，支持全选/取消

#### 输入
- arammayhem.com 英雄数据（id, tier, winRate, 多语言名称）
- 本地 heroes 表（id, name, title, meta_tier, win_rate）

#### 处理逻辑
1. 拉取 arammayhem 英雄列表
2. 通过英文 championId 匹配本地 heroes 表
3. 对比 meta_tier 和 win_rate 变化
4. 有变化 → 生成变更记录
5. 无变化 → 跳过

#### 输出
- heroes 表更新（meta_tier, win_rate）
- 前台 HeroInfo 组件展示胜率标签

---

### 4.3 英雄-符文匹配

#### 功能描述
基于 arammayhem.com 的666个英雄+符文组合数据，为每个英雄自动匹配推荐符文，生成推荐配置。

#### 用户界面
- 位置：管理后台 → AI智能体 → 英雄-符文匹配卡片
- 预览列表：英雄、符文、符文描述、推荐理由、匹配度、变更说明
- 筛选标签：全部/新增/更新/跳过
- S=100 / A=90 / B=85 / C=70 评分

#### 输入
- arammayhem.com 666 combos（championId, augmentIds[], tier, desc）
- 本地 heroes 表和 runes 表

#### 处理逻辑
1. 拉取 combo 数据
2. 匹配 championId → 本地 hero_id
3. 匹配 augmentId → 本地 rune_id
4. 为每个英雄自动创建「通用流派」playstyle
5. 将所有匹配符文归入该英雄的通用流派
6. 保护 source='manual' 的记录不被覆盖
7. 增量模式跳过已有记录，全量模式更新分数

#### 输出
- hero_rune_recommendations 表新增推荐记录
- hero_playstyles 表新增「通用流派」

---

### 4.4 AI 比对

#### 功能描述
使用 DeepSeek AI 分析官方和社区数据中名不同但描述相似的符文，判断是否为同一符文。

#### 用户界面
- 预览结果中新增「🤖 AI 比对」按钮
- 弹窗展示比对结果：判定（同/不同/不确定）、信心指数、推荐名称

#### 输入
- CDragon 符文中文名 + arammayhem 符文中文名
- 两者描述文本

#### 处理逻辑
1. 收集名不同但描述相似度高的符文对
2. 构建分析 prompt → 调用 DeepSeek
3. AI 返回判定结果（same/different/uncertain）+ 推荐名称 + 理由

#### 输出
- 弹窗展示 AI 比对结果
- 管理员据此手动确认或调整

---

### 4.5 更新日志管理

#### 功能描述
记录每次数据同步操作的详细信息，支持手动增删改日志条目。

#### 用户界面
- 位置：管理后台 → 更新日志标签
- 筛选：全部/符文同步/推荐生成/手动记录
- 展开查看详情（JSON 格式）
- 新增日志按钮（手动记录版本迭代等）

#### 处理逻辑
1. 同步执行时自动写入 update_logs 表
2. 预览不写入日志
3. 管理员可手动增删改日志

---

## 5. 掌上英雄联盟竞品分析

2026年2月，掌盟推出了「海斗助手」AI实时对局指导工具。

### 5.1 功能对比

| 功能 | 掌盟海斗助手 | 海克斯助手（本项目） |
|------|------------|-------------------|
| 实时符文推荐 | ✅ AI 实时分析 | ✅ 预置推荐+基准分推算 |
| 阵容分析 | ✅ 根据双方阵容 | ❌ 无（手动输入英雄） |
| 出装推荐 | ✅ 实时推荐 | ❌ 无 |
| 英雄胜率 | ✅ | ✅ arammayhem同步 |
| 离线可用 | ❌ 需联网 | ✅ PWA离线缓存 |
| AI幻觉问题 | ⚠️ 编造技能效果 | ✅ 数据来自权威源 |
| 响应速度 | ~10秒 | 即时（本地数据） |
| 广告 | ❌ 有广告 | ✅ 无广告 |

### 5.2 借鉴方向

1. **阶段感知**：掌盟助手根据游戏进程推荐符文，本项目可增加阶段计时器
2. **阵容分析**：未来可考虑输入双方阵容进行智能推荐（目前规则禁止调用外部API）
3. **简洁性**：掌盟的推荐界面非常简洁，本项目快速模式可参考

---

## 6. 用户操作性优化建议

### 6.1 核心原则

> **前台一定要让用户便于操作，辅助用户去完成一场游戏，而不是用户需要耗费大量时间研究该助手如何使用。**

### 6.2 优化清单

| 优先级 | 优化项 | 说明 |
|--------|--------|------|
| P0 | 阶段计时器 | 用户点"开始游戏"后自动计时，高亮当前阶段符文 |
| P0 | 快速模式开关 | 精简卡片展示，一屏看更多符文 |
| P0 | 技能强化符文标识 | 3.0核心新功能，需醒目展示 |
| P1 | 滑动排除 | 左右滑动快速排除符文 |
| P1 | 一键全选推荐 | 社区高分推荐的符文一键全选 |
| P1 | 游戏结束总结 | 记录本局选择，下次同英雄时提示 |
| P2 | 语音提示 | 阶段切换时浏览器通知提醒 |
| P2 | 暗黑模式优化 | 护眼模式，适合长时间游戏 |

### 6.3 用户操作流程（优化后）

```
打开助手 → 搜索/选择英雄 → 看英雄卡片（攻击类型+胜率）
    ↓
点击"开始游戏" → 启动阶段计时器
    ↓
Lv.3 开局 → 查看推荐符文（高亮当前阶段）→ 选择
    ↓ （滑动排除不好的，专注看好的）
Lv.7 → 高亮变化 → 选择
    ↓
Lv.11 → 选择
    ↓
Lv.15 → 选择
    ↓
游戏结束 → 记录总结
```

---

## 7. 行动计划

### 7.1 3.0上线前（6月11日前）

| 任务 | 优先级 | 预估工时 |
|------|--------|----------|
| 新增 `is_skill_augment` 字段 | P0 | 0.5h |
| 符文同步适配技能强化符文类别 | P0 | 1h |
| 移除羁绊相关代码和UI | P0 | 1h |
| 更新评分矩阵 | P1 | 0.5h |
| 阶段计时器前端开发 | P1 | 2h |

### 7.2 3.0上线后（6月11日当周）

| 任务 | 优先级 | 预估工时 |
|------|--------|----------|
| 首次同步3.0数据（符文池+英雄胜率+匹配） | P0 | 人工操作 |
| 验证37个删除符文已停用 | P0 | 0.5h |
| 验证63个新符文已录入 | P0 | 0.5h |
| 验证技能强化符文推荐逻辑 | P0 | 1h |
| arammayhem数据更新验证 | P1 | 0.5h |

### 7.3 持续优化

| 任务 | 优先级 | 预估工时 |
|------|--------|----------|
| 快速模式开关 | P1 | 2h |
| 滑动排除 | P2 | 3h |
| 游戏历史记录 | P2 | 4h |

---

> 📌 **报告结论**：现有系统对3.0版本具备良好的兼容性。核心同步功能无需大改，主要工作集中在羁绊移除、技能强化符文适配和前台用户体验优化。整体影响可控，预计3个工作日完成所有适配。
